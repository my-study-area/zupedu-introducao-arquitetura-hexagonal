version: '3.0'
services:
  db:
    image: postgres:14.3
    restart: always
    hostname: livraria
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=user
      - POSTGRES_DB=livraria
